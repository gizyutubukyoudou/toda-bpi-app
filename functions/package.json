{
  "name": "kaiki-app-functions",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "typecheck": "tsc --noEmit"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.2",
    "@sendgrid/mail": "^8.1.4"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.14.2"
  },
  "engines": {
    "node": "20"
  }
}
